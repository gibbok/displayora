# Implementation Review Reports

Copy `TEMPLATE.md` to `NN-<feature>-review.md` during implementation. Reports
are durable implementation evidence, not specification-authoring reviews.
Only a final report containing `Blocking findings remaining: 0` and
`Final verdict: Approved` permits `Verified` in `SPEC_STATUS.md`.
